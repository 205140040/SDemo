package com.demo.MavenHello;

public class Demo1 {

	public static void main(String[] args) {
		/**
		 * mvn 测试 mvn clean test
		 * mvn 打包：mvn clean mvn package
		 * mvn 安装到本地仓库，mvn clean install
		 */
		/**
		 * 1.导入maven项目:import - existing maven project,选择根目录即可
		 * 2.创建maven项目，选择骨架，快速开始的骨架，输入groupid，骨架id
		 * 3.编译器上运行maven命令，右键run as进行选择
		 * 4.编译器上运行maven 自定义命令,run as -maven bulid 在global中输入命令
		 * ，设置一下name，单击run即可:如global中输入clean test
		 */
	}

}
